import React from 'react';

function Me({name, age}) {
    return (
        <div>
            <h2>i am {name}</h2>
            <p>welcome to my website. I am {age} years old.</p>
        </div>
    );
};

export default Me;